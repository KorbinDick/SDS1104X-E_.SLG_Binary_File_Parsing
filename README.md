# SDS1104X-E-.SLG-Binary-File-Parsing
Includes python script that takes a Siglent SDS1104X-E generated binary file path and creates a CSV and plot containing the captured waveform and individual timestamps for captured samples (helpful when using NTP).
